<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CS2340_1">
        <activity
            android:name=".notification_activity"
            android:exported="false" />
        <activity
            android:name=".add_type_assignment"
            android:exported="true" />
        <activity
            android:name=".add_type_course"
            android:exported="true" />
        <activity
            android:name=".add_type"
            android:exported="true" />
        <activity
            android:name=".add_type_exam"
            android:exported="true" />
        <activity
            android:name=".splash_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".calendar_view" />
        <activity
            android:name=".to_do_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.cs2340_1.ACTION_TODO" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>